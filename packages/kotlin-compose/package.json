{
  "name": "@factoredui/kotlin-compose",
  "version": "0.2.1",
  "description": "Kotlin Multiplatform + Compose Multiplatform rendering engine for FactoredUI SDUI specs. Not an npm package — consumed directly via Gradle.",
  "private": true,
  "keywords": [
    "kotlin",
    "compose-multiplatform",
    "sdui",
    "server-driven-ui",
    "kmp"
  ],
  "scripts": {
    "build": "cd packages/kotlin-compose && ./gradlew assemble",
    "test": "cd packages/kotlin-compose && ./gradlew allTests"
  },
  "note": "This package is a Gradle subproject. It is listed here for monorepo discoverability only. Use Gradle commands directly — see README.md."
}
