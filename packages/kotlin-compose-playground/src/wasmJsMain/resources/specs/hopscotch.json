{
  "spec_version": 1,
  "renderer_min": 1,
  "root": {
    "id": "hopscotch",
    "type": "column",
    "props": { "gap": 4, "padding": 4 },
    "children": [
      { "id": "hs-title", "type": "text", "props": { "value": "Hopscotch — healthy (left) vs injured (right)", "variant": "heading" } },
      { "id": "hs-sub", "type": "text", "props": { "value": "Two real solver streams from il-injury, same clock. LEFT = healthy (hops on the right leg). RIGHT = right leg hurt, severity 1.0 → the body switches to land on the LEFT leg every square to spare the injury. The favor (which leg works) emerges from the pain cost — nothing authored. Kinematic reference; PHC adds flight dynamics downstream.", "variant": "caption" } },
      { "id": "hs-scene", "type": "scene3d", "props": { "clip_url": "clips/hopscotch_healthy_stream.json", "clip_url_b": "clips/hopscotch_injured_stream.json", "background": "neutral-gray", "flex": 1 } }
    ]
  }
}
